# 변경 이력

작업할 때마다 맨 위에 날짜를 추가하고, 한 줄에 하나씩 무엇을 왜 바꿨는지 짧게 적는다.
`(#38)` 은 PR 번호. 자세한 내용은 그 PR과 커밋을 보면 된다.

---

## 빠른 참고

**구조** — `index.html` 하나에 HTML·CSS·JS가 모두 들어 있다. 빌드 과정 없음. 파일 하나만 열어도(`file://`) 동작한다.

**배포** — `main`에 머지하면 GitHub Pages가 자동 배포한다 (약 15~45초). 주소: https://d2ceng.github.io/WEB-JA-Quiz/

**주의** — `index.html`을 바꾸면 `sw.js`의 `CACHE` 버전을 올린다. 안 올리면 기존 사용자에게 옛 화면이 남는다. `data/*.json`은 네트워크 우선이라 캐시 문제가 없다.

**단어장 데이터** — `data/` 아래 JSON. `data/wordbooks.json`이 앱에 보이는 목록이다. 파일을 추가하면 이 목록에도 넣어야 한다.

**브라우저 저장소 키**
| 키 | 내용 |
|---|---|
| `jaquiz.v1` | 기본(main) 단어장 슬롯 — 단어·진도·일정·설정·학습일지 |
| `jaquiz.v1.book.<id>` | 추가 단어장 슬롯 (구성은 위와 같음) |
| `jaquiz.v1.books` | 단어장 목록과 현재 활성 단어장 |
| `jaquiz.v1.sync` | 동기화 메타 — 올릴 항목 표시, 조각 수, 단어 목록 해시 |
| `jaquiz.v1.quiz` | 진행 중인 시험 (새로고침 후 복원용) |
| `jaquiz.lang` | 화면 언어 |

**클라우드(Firestore)** — `users/{uid}` 에 단어장 목록, `users/{uid}/books/{id}` 에 단어장 정보. 단어는 `{id}~w{i}`, 진도는 `{id}~s{i}` 조각에 나눠 저장한다. 학습 중에는 진도 조각만 올라간다. 보안 규칙은 README 참고.

**확인 방법** — `python3 -m http.server 4173` 으로 띄우고 브라우저(또는 Playwright)로 확인한다. 로그인이 필요한 동작은 Firestore를 흉내 낸 스텁으로 시험했다.

---

## 2026-09-04

- 새 일정을 만들면 오늘 외운 기록이 캘린더에서 사라지던 문제를 고쳤다. 이미 외운 단어는 배웠던 날짜에 그대로 남긴다. (#38)
- 새 일정에는 아직 못 외운 단어만 넣는다. 이미 외운 단어를 다시 배치하면 오지도 않은 날이 완료로 보였다. (#38)
- 퀴즈 상단의 `점수 1 / 8` 이 진행도처럼 읽혀서 `진행 1 / 8` 로 바꿨다. 진행 막대도 푼 문제 수 기준. 점수는 결과 화면에 나온다. (#37)
- `.btn` 의 display가 `hidden` 속성을 덮어써서 숨긴 버튼이 빈 상자로 남던 문제를 고쳤다. (#37)

## 2026-09-03

- 일정·암기·퀴즈 탭 위쪽에 단어장 선택기를 뒀다. 단어 DB 탭까지 가지 않아도 바꿀 수 있다. (#36)
- 일정 탭 맨 위에 단어장별 오늘 할 분량 요약을 넣었다. 누르면 그 단어장으로 넘어간다. (#36)
- 동기화를 단어 목록과 진도로 나눴다. 학습 중에는 진도만 올라가서 전송량이 크게 줄었다. 옛 형식도 읽고 다음 업로드 때 새 형식으로 옮긴다. (#35)
- 퀴즈에 '이전 모드로 돌아가기' 버튼을 넣었다. 실수로 모드를 바꿨을 때 확인 창 없이 한 번에 돌아온다. (#35)
- 퀴즈에 문제 이동(◀ 이전 / 다음 ▶)을 넣었다. 이미 푼 문제로 돌아가면 결과만 보여주고 다시 채점하지 않는다. (#34)
- 시험 도중 모드를 바꾸면 확인 창을 띄워 취소할 수 있게 했다. (#34)
- 새로고침하거나 앱을 다시 열어도 풀던 시험을 이어서 풀 수 있게 했다. (#34)
- 인터넷이 없으면 업로드를 미루고 연결되면 자동으로 올린다. 실패하면 간격을 늘려 가며 다시 시도한다. (#34)
- 시험 중 4지선다·주관식·말하기를 오가도 진행 내역이 사라지지 않게 했다. 단계마다 따로 보관한다. (#33)

## 2026-08-20

- 일정 기간을 늘렸는데 하루 분량이 오히려 늘던 문제를 고쳤다. 원인이 둘이었다. 재배치에 이미 외운 단어가 포함됐고, 평일/주말 배수를 '남는 자리가 적은' 기준으로 골랐다. (#32)
- 퀴즈에서 한자 표기가 없는 단어(`そして`, `テレビ`)를 漢字↔ひらがな 방향 출제에서 뺐다. 문제가 곧 답이었다. (#32)

## 2026-07-29

- 저장소에 단어장을 추가해도 기존 사용자에게 안 보이던 문제를 고쳤다. 서비스워커가 `data/*.json`을 캐시 우선으로 주고 있었다. 네트워크 우선으로 바꿨다. (#31)

## 2026-07-21

- 단어장 5종을 추가했다: 마트 장보기, 편의점, 드러그스토어, 전철·교통, 공항·호텔. 이자카야 단어장도 갱신했다. 총 12종. (#30)
- 단어장 4종을 추가했다: 조수사, 경어, 의성어·의태어, 온천·료칸. (#29)
- JLPT N5–N1 일영 단어장(9,902단어)을 추가했다. (#28)

## 2026-07-14

- 탭 순서에서 문법을 시험지 인쇄 앞으로 옮겼다. 모바일에서 상단 버튼들을 제목 옆줄로 붙였다. (#27)
- 같은 기기에서 다른 계정으로 로그인하면 클라우드 데이터를 안 내려받던 문제를 고쳤다. 계정이 바뀌면 동기화 이력을 지운다. (#26)
- 앱을 켜둔 동안에도 다른 기기의 변경을 자동으로 받아오게 했다. 화면 복귀·5분 주기로 가볍게 확인한다. (#26)
- 모바일에서 동기화 팝업이 화면 밖으로 잘리던 문제를 고쳤다. (#25)
- 큰 단어장이 Firestore 1MB 한도에 걸려 업로드가 실패하던 문제를 고쳤다. 조각으로 나눠 저장한다. (#25)
- 클라우드 동기화를 도입했다. Google 로그인 후 단어장·진도가 기기 간에 동기화된다. (#24)

---

이전(#23 이하) 기록은 `git log` 참고. 그때까지는 브라우저 저장소만 쓰는 로컬 전용 앱이었다.
